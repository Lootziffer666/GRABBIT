name: grabbit_core
description: UI-free core for GRABBIT — file index, queries, actions. No Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies: {}

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0
